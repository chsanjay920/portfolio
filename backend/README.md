## portfolio-api

Node/Express backend for the dynamic portfolio CMS.

### Setup

1. Copy `.env.example` to `.env` and fill values.
2. Install deps:

```bash
npm install
```

3. Run dev server:

```bash
npm run dev
```

Health check: `GET /api/health`

